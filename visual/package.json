{
  "name": "roos-visual",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Visual regression baseline for the roos.com migration (Phase 0).",
  "scripts": {
    "postinstall": "playwright install chromium",
    "probe": "node probe.mjs",
    "baseline": "node run.mjs baseline",
    "visual": "node run.mjs visual",
    "visual:update": "node run.mjs update",
    "report": "playwright show-report report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  },
  "dependencies": {
    "roosinst": "file:.."
  }
}
